#include<stdio.h>

/**
 * main - entry point
 * Return: 0 (Success)
 *
 */
int main(void)
{

	int alpha;

	for (alpha = 'a'; alpha = 'z'; alpha++)
	{
		putchar(alpha);
	{

	putchar('\n');
	return (0);
}
