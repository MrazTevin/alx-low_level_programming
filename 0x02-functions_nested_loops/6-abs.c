#include "main.h"

/**
 * _abs - compute the absolute value of an integer
 * @n: integer to be used
 * Return: Always 0
 */

int _abs(int n)
{

	if (n < 0)
	{
		return (-n);
	}
	else
	{
		return (n);
	}

	return (0);

}
