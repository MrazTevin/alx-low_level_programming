#include "main.h"

/**
 * _abs - compute the absolute value of an integer
 * @int: data type
 * Return: Always 0
 */

int _abs(int)
{
	int n;

	if (n < 0)
	{
		return (-n);
	}
	else
	{
		return (n);
	}

	return (0);

}
