#include<stdio.h>

/**
 * main - entry point
 * Return: Always 0 (Success)
 * description: a program that prints with the puts function
 */
int main(void)
{
	char *s = "\"Programming is like building a multilingual puzzle";

	puts(s);

	return (0);
}
