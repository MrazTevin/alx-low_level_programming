#include<stdio.h>

/**
 * main - Entry point
 * Return: Always 0 (Success)
 * Description: print statement with printf
 */
int main(void)
{
	char *ptr = "with proper grammar, but the outcome is a piece of art,\n";

	printf("%s", ptr);

	return (0);
}
